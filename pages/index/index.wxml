<!--index.wxml-->
<view class="container">

<image wx:for="{{items}}" wx:key="{{index}}"
style="left:{{item.left}}rpx;top:{{item.top}}rpx"  
 mode="aspectFit" src="{{item.image}}" class="flower" 
 animation="{{animationData}}"></image>
</view>
